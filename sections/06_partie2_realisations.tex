\part{Réalisations Techniques, Résolution de la Problématique}
\label{part:realisations}

\chapter{Intégration Hybride des Protocoles GDS et IATA NDC}
\label{chap:integration_gds_ndc}

\section{Expérience Utilisateur et Moteur de Recherche Metis Connect}

Dès les premières phases de développement sur le moteur de recherche Metis Connect, un défi d'ingénierie s'est imposé : comment présenter dans une même interface des offres provenant de sources totalement divergentes sans ralentir l'opérateur ? D'un côté, les agents de voyages ont besoin d'une vue graphique claire pour comparer les tarifs en quelques clics ; de l'autre, les billettistes chevronnés exigent de conserver leurs habitudes de saisie en commandes cryptiques Sabre pour aller vite.

La figure \ref{fig:metis_recherche} présente l'interface principale de recherche de vols développée avec Nuxt~3 et Vue~3 au sein du composant \texttt{front-reservation}.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{assets/metis_recherche_vols_hd.png}
\caption[Interface de recherche de vols Metis Connect]{Interface de recherche multi-critères de la plateforme Metis Connect (\texttt{front-reservation})}
\label{fig:metis_recherche}
\end{figure}

Cette interface gère les allers simples, allers-retours et trajets multi-destinations. L'utilisateur y renseigne la composition de la délégation (adultes \texttt{ADT}, enfants \texttt{CHD}, bébés sans siège \texttt{INF}) et applique les tarifs publics ou négociés par l'agence. Des filtres dynamiques par compagnies et alliances permettent d'ajuster l'affichage en temps réel.

\section{Couche d'Abstraction Canonique via le Patron Adaptateur}

Pour alimenter cette interface sans multiplier les conditions spécifiques à chaque compagnie dans le frontend, j'ai mis en place une couche d'adaptateurs. Sabre renvoie des enveloppes SOAP ou du texte brut, tandis que les passerelles NDC directes (Air France-KLM, British Airways, Aegean, Turkish) répondent en JSON ou en XML.

Le module \texttt{SabreAdapter.ts} (listing \ref{lst:sabreadapter}) transforme ces données hétérogènes vers un modèle pivot commun, \texttt{CanonicalFlightOffer}. Grâce à ce patron de conception, les composants de recherche et le panier d'achat manipulent une structure de données TypeScript unique, sans dépendre des protocoles des transporteurs.

\begin{lstlisting}[language=TypeScript, caption={Adaptateur de normalisation des offres Sabre (\texttt{SabreAdapter.ts})}, label={lst:sabreadapter}]
export interface SabreFlightLeg {
  origin: string;
  destination: string;
  departureDateTime: string;
  arrivalDateTime: string;
  carrierCode: string;
  flightNumber: string;
  cabinClassCode: string; // 'Y', 'J', 'F', 'W', etc.
  bookingClass: string;
  operatingCarrier?: string;
}

export interface SabreAncillaryService {
  serviceId: string;
  serviceType: 'BAGGAGE' | 'SEAT' | 'MEAL' | 'PRIORITY';
  commercialName: string;
  amount: number;
  currency: string;
  segmentRefIds: string[];
}

export interface CanonicalFlightOffer {
  offerId: string;
  provider: 'SABRE_GDS' | 'SABRE_NDC';
  validatingCarrier: string;
  segments: Array<{
    id: string;
    origin: string;
    destination: string;
    departure: string;
    arrival: string;
    airline: string;
    flightNumber: string;
    cabin: 'ECONOMY' | 'PREMIUM' | 'BUSINESS' | 'FIRST';
  }>;
  ancillaries: SabreAncillaryService[];
  pricing: {
    baseFare: number;
    taxes: number;
    total: number;
    currency: string;
    taxBreakdown: Record<string, number>;
  };
}

export class SabreAdapter {
  public static mapCabin(code: string): 'ECONOMY' | 'PREMIUM' | 'BUSINESS' | 'FIRST' {
    const mapping: Record<string, 'ECONOMY' | 'PREMIUM' | 'BUSINESS' | 'FIRST'> = {
      'Y': 'ECONOMY', 'M': 'ECONOMY', 'K': 'ECONOMY', 'L': 'ECONOMY',
      'W': 'PREMIUM', 'E': 'PREMIUM',
      'J': 'BUSINESS', 'C': 'BUSINESS', 'D': 'BUSINESS',
      'F': 'FIRST', 'P': 'FIRST'
    };
    return mapping[code.toUpperCase()] || 'ECONOMY';
  }

  public static normalizeFlightOffer(raw: any): CanonicalFlightOffer {
    if (!raw?.id || !Array.isArray(raw?.flights)) {
      throw new Error("[SabreAdapter] Payload invalide : " + String(raw?.id));
    }

    const segments = raw.flights.map((leg: SabreFlightLeg, idx: number) => ({
      id: 'SEG_' + (idx + 1),
      origin: leg.origin,
      destination: leg.destination,
      departure: leg.departureDateTime,
      arrival: leg.arrivalDateTime,
      airline: leg.carrierCode,
      flightNumber: leg.carrierCode + leg.flightNumber,
      cabin: this.mapCabin(leg.cabinClassCode)
    }));

    const taxDetails: Record<string, number> = {};
    if (Array.isArray(raw.taxes)) {
      raw.taxes.forEach((tax: { code: string; amount: number }) => {
        taxDetails[tax.code] = (taxDetails[tax.code] || 0) + Number(tax.amount);
      });
    }

    return {
      offerId: String(raw.id),
      provider: raw.isNdc ? 'SABRE_NDC' : 'SABRE_GDS',
      validatingCarrier: raw.validatingCarrier || raw.flights[0].carrierCode,
      segments,
      ancillaries: (raw.services || []).map((srv: any) => ({
        serviceId: srv.id,
        serviceType: srv.type || 'BAGGAGE',
        commercialName: srv.name || 'Service Optionnel',
        amount: Number(srv.price?.total || 0),
        currency: srv.price?.currency || 'EUR',
        segmentRefIds: srv.flightRefs || []
      })),
      pricing: {
        baseFare: Number(raw.pricing?.baseFare || 0),
        taxes: Number(raw.pricing?.totalTax || 0),
        total: Number(raw.pricing?.totalAmount || 0),
        currency: raw.pricing?.currency || 'EUR',
        taxBreakdown: taxDetails
      }
    };
  }
}
\end{lstlisting}

\section{Terminal et Commandes Cryptiques pour Billettistes}

Au sein des agences de voyages traditionnelles, de nombreux billettistes experts possèdent une grande vélocité sur les commandes cryptiques du GDS Sabre. Ces formats historiques textuels permettent d'accomplir des opérations complexes en quelques frappes clavier. Afin de satisfaire cet usage métier sans contraindre les opérateurs à basculer constamment entre Metis Connect et un émulateur de terminal externe, j'ai intégré un \textbf{émulateur de terminal cryptique} directement au sein de l'environnement applicatif web.

La figure \ref{fig:metis_cryptique} illustre la modale d'exécution de commandes cryptiques et de restitution des réponses Sabre dans Metis Connect.

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{assets/metis_commande_cryptique.png}
\caption[Émulateur de terminal cryptique Sabre]{Émulateur de terminal et réponse cryptique Sabre intégrés dans Metis Connect}
\label{fig:metis_cryptique}
\end{figure}

Ce composant permet à l'agent de saisir des commandes textuelles natives pour interroger les disponibilités de classes, manipuler directement les champs d'un PNR ou gérer les profils d'imprimantes billettiques. Le service backend \texttt{api-aerial} achemine ces instructions via l'API Sabre Command et renvoie le flux textuel brut en temps réel dans l'interface, assurant une transition fluide entre l'ergonomie moderne du Web et l'efficacité des outils historiques.

\section{Gestion Découplée des Segments Passifs Amadeus et Sabre}

Lorsqu'un billet d'avion est émis au travers d'un canal direct NDC, la transaction est enregistrée directement dans le système central de la compagnie sans passer par l'inventaire actif du GDS de l'agence. Toutefois, afin que le progiciel de gestion et de facturation comptable de l'agence de voyages (tel que Gestour ou IEnterprise) puisse générer la facture client et intégrer les flux de trésorerie, un \textbf{segment passif} informatif doit être inscrit dans le GDS de référence de l'agence.

Une défaillance dans cette orchestration peut engendrer des doubles écritures passives ou des écritures incohérentes, sanctionnées immédiatement par les transporteurs sous la forme d'avis de débit (\textit{Agency Debit Memos} ou ADM) assortis de pénalités financières. Pour neutraliser ce risque, j'ai développé une migration de schéma de base de données (\texttt{split-passive-segment-options}) isolant strictement les paramètres d'écriture passive par point de vente. En amont de toute notification, le contrôleur backend évalue le statut d'émission du dossier : si la réservation est traitée sur le GDS Sabre, toute écriture passive redondante sur Amadeus est automatiquement désactivée, garantissant l'unicité comptable du dossier.

\section{Synchronisation des Dossiers Voyageurs (PNR)}

Le cycle de vie opérationnel d'un dossier de transport s'étend sur plusieurs semaines ou mois et subit régulièrement des modifications exogènes, telles que des réajustements d'horaires par les compagnies ou des demandes d'annulation partielle. Au sein de \texttt{retrieve.controller.js}, j'ai mis en place la fonction \texttt{keepOnlyMatchingTravelers}. Ce module assure une réconciliation stricte entre les données passagers rafraîchies depuis le GDS distant et l'état persisté dans la base locale, évitant toute corruption d'enregistrements lors d'accès concurrents par plusieurs agents.

\chapter{Cycle Transactionnel de Réservation et Retarification Temps Réel}
\label{chap:booking_reshop}

\section{Détection et Gestion des Écarts Tarifaires (Reshopping)}

Dans le secteur aérien, le montant affiché lors d'une recherche ne constitue qu'une estimation indicative : le prix n'est contractuellement garanti par le transporteur qu'au moment précis de l'émission effective du billet électronique. Durant l'intervalle où l'utilisateur complète les formulaires d'identification des voyageurs, les algorithmes de \textit{Revenue Management} de la compagnie peuvent fermer le palier tarifaire sélectionné ou réajuster le barème des taxes aéroportuaires et surcharges carburant.

Pour absorber cette volatilité sans infliger un échec bloquant à l'utilisateur, j'ai conçu un cycle de \textbf{retarification réactive} (\textit{Reshopping / Reprice}) dont l'orchestration asynchrone est illustrée par le diagramme de séquence de la figure \ref{fig:sequence_reprice}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    scale=0.82, transform shape,
    font=\sffamily,
    actor/.style={rectangle, rounded corners=4pt, draw=CobaltBlue!45, fill=CobaltBlue!8, text=NavyBlue, font=\small\bfseries, align=center, inner sep=6pt, line width=0.8pt, minimum width=3.4cm},
    msg/.style={->, >=stealth, color=CobaltBlue, line width=0.8pt},
    reply/.style={->, >=stealth, dashed, color=AlertRed, line width=0.8pt},
    okreply/.style={->, >=stealth, dashed, color=CobaltBlue, line width=0.8pt},
    note/.style={rectangle, rounded corners=4pt, draw=AccentGreen!55!black, fill=AccentGreen!10, text=NavyBlue, font=\scriptsize, align=center, inner sep=5pt, line width=0.8pt},
    lifeline/.style={draw=DarkSlate!30, dashed, line width=0.6pt}
]

% Participants
\node[actor] (client) {Client Web / SBT\\[1pt]\scriptsize\texttt{front-reservation}};
\node[actor, right=2.8cm of client] (server) {Serveur Métier Metis\\[1pt]\scriptsize\texttt{api-aerial}};
\node[actor, right=2.8cm of server] (supplier) {Fournisseur Aérien\\[1pt]\scriptsize GDS Sabre / NDC Direct};

% Coordinates for Lifelines
\coordinate (c_bottom) at ($ (client.south) + (0,-8.7) $);
\coordinate (s_bottom) at ($ (server.south) + (0,-8.7) $);
\coordinate (p_bottom) at ($ (supplier.south) + (0,-8.7) $);

\draw[lifeline] (client.south) -- (c_bottom);
\draw[lifeline] (server.south) -- (s_bottom);
\draw[lifeline] (supplier.south) -- (p_bottom);

% Step 1
\draw[msg] ($ (client.south) + (0,-0.6) $) -- node[above, font=\scriptsize\bfseries\color{DarkSlate}] {1. Validation Panier / Checkout} ($ (server.south) + (0,-0.6) $);

% Step 2
\draw[msg] ($ (server.south) + (0,-1.3) $) -- node[above, font=\scriptsize\bfseries\color{DarkSlate}] {2. Re-shopping inventaire en temps réel} ($ (supplier.south) + (0,-1.3) $);

% Step 3
\draw[okreply] ($ (supplier.south) + (0,-2.0) $) -- node[above, font=\scriptsize\color{AlertRed}] {3. Tarif actualisé ($P_1 \neq P_0$) / Changement classe} ($ (server.south) + (0,-2.0) $);

% Step 4
\draw[reply] ($ (server.south) + (0,-2.8) $) -- node[above, font=\scriptsize\bfseries\color{AlertRed}] {4. Statut HTTP 409 Conflict + Payload Offre révisée ($P_1$)} ($ (client.south) + (0,-2.8) $);

% Step 5 & 6 (Client UI Modal Note)
\node[note, text width=5.2cm] at ($ (client.south) + (0,-4.0) $) (modal) {\textbf{\textcolor{NavyBlue}{\texttt{PriceChangeConfirmDialog.vue}}}\\[2pt]5. Affichage ventilation écart tarifaire\\6. Consentement explicite du voyageur ($P_1$)};

% Step 7
\draw[msg] ($ (client.south) + (0,-5.6) $) -- node[above, font=\scriptsize\bfseries\color{CobaltBlue}] {7. Confirmation commande avec \texttt{OfferID} révisé} ($ (server.south) + (0,-5.6) $);

% Step 8
\draw[msg] ($ (server.south) + (0,-6.3) $) -- node[above, font=\scriptsize\bfseries\color{DarkSlate}] {8. Requête d'émission ferme (\texttt{jsonOrderCreateRQ})} ($ (supplier.south) + (0,-6.3) $);

% Step 9
\draw[okreply] ($ (supplier.south) + (0,-7.0) $) -- node[above, font=\scriptsize\bfseries\color{CobaltBlue}] {9. Billet électronique émis + PNR confirmé} ($ (server.south) + (0,-7.0) $);

% Step 10
\draw[okreply] ($ (server.south) + (0,-7.8) $) -- node[above, font=\scriptsize\bfseries\color{CobaltBlue}] {10. Réponse HTTP 200 OK (PNR \& Titre de transport émis)} ($ (client.south) + (0,-7.8) $);

\end{tikzpicture}
\caption{Orchestration asynchrone de la retarification lors de la commande de billet}
\label{fig:sequence_reprice}
\end{figure}

Le processus transactionnel s'exécute de manière progressive et déterministe. Dès que l'utilisateur confirme son intention d'achat, le service \texttt{api-aerial} soumet une requête de repricing (\texttt{repriceOffer}) au fournisseur concerné. Si le coût total a fluctué ($P_1 \neq P_0$), le serveur backend rejette la création immédiate en renvoyant un statut HTTP 409 (\textit{Conflict}), enrichi de la nouvelle grille tarifaire et d'un jeton d'offre révisé.

L'application frontend intercepte cette réponse d'erreur métier et déclenche l'affichage du composant modale \texttt{PriceChangeConfirmDialog.vue}, présenté en figure \ref{fig:modal_price_change}. Ce composant expose de manière limpide la décomposition de l'écart constaté ($\Delta P = P_1 - P_0$) entre tarif de base et taxes. Lorsque le voyageur valide ce réajustement, la commande est rejouée avec l'identifiant \texttt{OfferID} révisé, en réutilisant l'état complet des formulaires passagers conservé dans le store Pinia \texttt{useBookingStore}. En cas de refus, le verrou du panier est levé et l'utilisateur est réorienté en douceur vers la liste des résultats de recherche.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    scale=0.92, transform shape,
    font=\sffamily,
    dialog/.style={rectangle, rounded corners=8pt, draw=BorderGray, fill=white, line width=1.1pt, drop shadow={opacity=0.15}},
    btnprimary/.style={rectangle, rounded corners=4pt, fill=CobaltBlue, text=white, font=\footnotesize\bfseries, inner sep=6pt},
    btnsecondary/.style={rectangle, rounded corners=4pt, draw=DarkSlate!40, fill=white, text=DarkSlate, font=\footnotesize, inner sep=6pt}
]

\node[dialog, text width=13.0cm, inner sep=12pt] (d) {
    % Header
    {\large\textbf{\color{NavyBlue}Ajustement tarifaire du transporteur}}\\[2pt]
    {\footnotesize\color{DarkSlate}Vol Aller : Paris Charles-de-Gaulle (CDG) $\rightarrow$ New York (JFK) · Air France AF006}\\[6pt]
    \arrayrulecolor{CobaltBlue!25}
    \begin{tabularx}{\linewidth}{X r}
        \hline
        \rowcolor{AlertRed!8}
        \multicolumn{2}{p{12.4cm}}{\footnotesize\textcolor{AlertRed}{\textbf{Avis transporteur :} L'inventaire de la classe sélectionnée a été réévalué. Le prix du billet a été actualisé sans rupture de panier.}} \\
        \hline
        \textbf{Tarif initialement affiché (recherche) :} & \textcolor{DarkSlate}{432,00\,€} \\
        \textbf{Nouveau tarif réévalué (Air France NDC) :} & \textbf{\color{NavyBlue}446,50\,€} \\
        \hline
        \textbf{Écart tarifaire total ($\Delta P$) :} & \textbf{\color{AlertRed}+14,50\,€} \\
        \multicolumn{2}{p{12.4cm}}{\scriptsize\textcolor{DarkSlate}{\textit{Ventilation : +0,00\,€ sur le tarif de base, +14,50\,€ sur la surtaxe carburant YQ.}}} \\
        \hline
    \end{tabularx}
    \\[6pt]
    {\scriptsize\color{DarkSlate}\textbf{--} Les coordonnées saisies pour les 2 passagers sont conservées dans \texttt{useBookingStore}.\\[1pt]
    \textbf{--} L'offre révisée (\texttt{OfferID: AF-789-REV}) expire dans \textbf{04:38}.}
    \\[8pt]
    \begin{flushright}
        \begin{tikzpicture}[baseline=(b1.base)]
            \node[btnsecondary] (b1) {Annuler / Modifier recherche};
            \node[btnprimary, right=0.3cm of b1] (b2) {Confirmer le nouveau tarif (446,50\,€)};
        \end{tikzpicture}
    \end{flushright}
};

\end{tikzpicture}
\caption[Dialogue de confirmation d'écart tarifaire]{Interface du dialogue de confirmation d'écart tarifaire (\texttt{PriceChangeConfirmDialog.vue})}
\label{fig:modal_price_change}
\end{figure}

\section{Algorithme Déterministe de Réconciliation des Passagers}

Au début du projet, nous avions régulièrement des rejets de commande sur l'API NDC d'Air France ou de British Airways. En examinant les réponses XML d'erreur, j'ai constaté que le problème venait souvent de détails de saisie : un prénom avec accent (ex: « Éléonore »), un nom avec apostrophe (ex: « D'ALMEIDA »), ou un bébé sans siège dont la balise de rattachement à un adulte manquait. De plus, quand un utilisateur retirait un voyageur du panier, les options de bagages restaient parfois liées à un identifiant fantôme.

Pour corriger ces anomalies, j'ai développé l'algorithme \texttt{matchesPaxRefId} dans le fichier \texttt{orderCreation.utils.ts} (listing \ref{lst:matchespaxrefid}). Il nettoie les noms en ASCII 7-bit, lie chaque nourrisson à un adulte (\texttt{associatedAdultRefId}) et purge les services orphelins.

\begin{lstlisting}[language=TypeScript, caption={Algorithme de réconciliation des passagers et gestion des cas limites (\texttt{orderCreation.utils.ts})}, label={lst:matchespaxrefid}]
export interface RawTravelerInput {
  id?: string;
  type: 'ADT' | 'CHD' | 'INF';
  firstName: string;
  lastName: string;
  dateOfBirth?: string;
  assignedAdultIndex?: number; // Requis si type === 'INF'
}

export interface NormalizedPassenger {
  paxRefId: string;
  ptc: 'ADT' | 'CHD' | 'INF';
  givenName: string;
  surname: string;
  birthDate?: string;
  associatedAdultRefId?: string;
}

export function sanitizeIataName(name: string): string {
  return name
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '') // Supprime accents et diacritiques
    .replace(/[^a-zA-Z\s]/g, ' ')   // Remplace tirets et apostrophes par espaces
    .replace(/\s+/g, ' ')           // Reduit les espaces multiples
    .trim()
    .toUpperCase();
}

export function matchesPaxRefId(
  travelers: RawTravelerInput[],
  selectedAncillaries: Array<{ serviceId: string; targetPaxId: string }>
): {
  passengers: NormalizedPassenger[];
  validAncillaries: Array<{ serviceId: string; paxRefId: string }>;
} {
  if (!travelers || travelers.length === 0) {
    throw new Error('[matchesPaxRefId] Aucun voyageur fourni pour la commande');
  }

  const adultRefIds: string[] = [];
  const normalizedPaxList: NormalizedPassenger[] = [];

  // 1. Passe 1 : Traitement et enregistrement des Adultes et Enfants
  travelers.forEach((pax, index) => {
    const generatedRefId = 'PAX' + (index + 1);
    const cleanFirst = sanitizeIataName(pax.firstName);
    const cleanLast = sanitizeIataName(pax.lastName);

    if (pax.type === 'ADT') {
      adultRefIds.push(generatedRefId);
    }

    if (pax.type !== 'INF') {
      normalizedPaxList.push({
        paxRefId: generatedRefId,
        ptc: pax.type,
        givenName: cleanFirst,
        surname: cleanLast,
        birthDate: pax.dateOfBirth
      });
    }
  });

  // 2. Passe 2 : Traitement des Nourrissons (INF) et rattachement adulte
  travelers.forEach((pax, index) => {
    if (pax.type === 'INF') {
      const generatedRefId = 'PAX' + (index + 1);
      const adultTargetRef = adultRefIds[pax.assignedAdultIndex ?? 0] || adultRefIds[0];

      if (!adultTargetRef) {
        throw new Error('[matchesPaxRefId] Nourrisson sans adulte valide : ' + pax.firstName);
      }

      normalizedPaxList.push({
        paxRefId: generatedRefId,
        ptc: 'INF',
        givenName: sanitizeIataName(pax.firstName),
        surname: sanitizeIataName(pax.lastName),
        birthDate: pax.dateOfBirth,
        associatedAdultRefId: adultTargetRef
      });
    }
  });

  // 3. Purge des services ancillaires orphelins
  const validPaxIdSet = new Set(normalizedPaxList.map(p => p.paxRefId));
  const validAncillaries = (selectedAncillaries || [])
    .filter(anc => validPaxIdSet.has(anc.targetPaxId))
    .map(anc => ({ serviceId: anc.serviceId, paxRefId: anc.targetPaxId }));

  return {
    passengers: normalizedPaxList,
    validAncillaries
  };
}
\end{lstlisting}

\chapter{Optimisation du Moteur de Recherche et Filtrage Multi-Compagnies}
\label{chap:moteur_recherche}

\section{Composant de Filtrage et Persistance des Préférences}

Dans un outil de réservation d'entreprise, les gestionnaires de voyages doivent souvent restreindre les résultats selon les contrats passés avec certaines alliances (SkyTeam, Star Alliance, Oneworld) ou exclure certaines compagnies low-cost.

Pour gérer ces filtres sans ralentir l'interface, le composant Vue~3 \texttt{SelectCompanies.vue} propose deux modes :
une liste blanche pour cibler les compagnies conventionnées, et une liste noire pour écarter certains transporteurs tout en interrogeant le reste du marché.

Pour que l'utilisateur ne perde pas ses filtres à chaque rechargement de page, nous sauvegardons ces préférences à la fois dans le store Pinia (pour la session en cours) et dans le \texttt{localStorage} du navigateur. Cela évite de faire des requêtes en base de données pour de simples préférences d'affichage.

\section{Optimisation des Requêtes de Recherche Serveur}

Le point critique sur les performances reste les temps de réponse des fournisseurs externes. Dans \texttt{api-aerial}, la fonction \texttt{createShoppingPayload} (\texttt{shopping.utils.js}) a été optimisée pour filtrer les compagnies exclues avant même de construire la requête XML/JSON. Plutôt que d'interroger tous les connecteurs pour ensuite jeter les résultats non souhaités, nous n'appelons que les fournisseurs pertinents. Cela permet de gagner plusieurs centaines de millisecondes sur les recherches complexes.

Nous avons également ajouté deux optimisations concrètes : l'injection du paramètre \texttt{'W'} dans les requêtes Sabre pour forcer la recherche de vols directs (très demandés par les voyageurs d'affaires), et la normalisation stricte des dates en ISO 8601 (\texttt{createDepartureWindow}) pour éviter que Sabre et les API NDC n'interprètent différemment les heures de départ.

\chapter{Architecture Financière et Traitement Asynchrone Multi-Devises}
\label{chap:architecture_financiere}

\section{Résolution Asynchrone de la Devise Point de Vente}

En arrivant sur le module de facturation et de conversion monétaire, nous nous sommes heurtés à un mur de performance. Les requêtes SQL synchrones qui résolvaient la devise de chaque Point de Vente (\texttt{agencyCurrency}) saturaient le pool de connexions dès que plusieurs agences lançaient des recherches en même temps. Un paramètre quasi statique bloquait tout le pipeline tarifaire.

Pour débloquer la situation, j'ai déporté cette résolution dans un middleware asynchrone Express avec un cache mémoire de dix minutes. La devise de l'agence est lue une seule fois au début de la requête, puis injectée dans \texttt{req.context.posCurrency} pour être réutilisée sans aucun appel SQL supplémentaire. Cela a éliminé la contention sur la base de données tout en divisant le temps de traitement de ces calculs.

Ce choix n'est pas idéal dans l'absolu : une invalidation immédiate par Redis ou Webhook aurait été plus élégante. Mais cela aurait nécessité une infrastructure supplémentaire que nous ne jugions pas justifiée pour une donnée modifiée une fois par an par les administrateurs. J'ai donc privilégié la simplicité d'un cache mémoire Node.js avec une durée de dix minutes.

\section{Formatage Dynamique et Norme ISO 4217}

La gestion des devises multiples impose une conformité stricte avec la norme ISO 4217. Les calculs dans \texttt{formatPrice} et l'affichage dans \texttt{OrderRecap.vue} doivent s'adapter aux particularités monétaires : les devises courantes à deux décimales (EUR, USD, GBP), les monnaies sans décimale (JPY, KRW) et les monnaies à trois décimales (TND, KWD).

Pour éviter les écarts cumulatifs lors de l'addition des taxes aéroportuaires, nous appliquons l'arrondi bancaire au pair le plus proche (\textit{banker's rounding}). Contrairement à l'arrondi classique qui arrondit toujours les demi-unités vers le haut et fausse les totaux sur de grands volumes, l'arrondi bancaire alterne vers le chiffre pair le plus proche. Cela garantit l'égalité arithmétique entre le total facturé et la somme des taxes, sans décalage de centime.

\chapter{Back-Office d'Administration et Gouvernance des Agences}
\label{chap:backoffice_admin}

\section{Interface de Paramétrage des Points de Vente}

Développée avec Nuxt~3 et Vuetify~3, l'application d'administration \textbf{\texttt{front-dashboard}} permet aux administrateurs de configurer les accès techniques de chaque agence.

L'administrateur y renseigne les identifiants indispensables aux transactions : le PCC Sabre (\textit{Pseudo City Code}), l'Office ID Amadeus et le numéro d'accréditation IATA. Chaque réseau ayant ses propres contrats et taux de commission, l'interface permet de définir la devise par défaut du point de vente, la marge appliquée sur les billets et l'activation des segments passifs selon les besoins comptables de l'agence.

\section{Synchronisation par Lots des Profils Voyageurs Sabre}

L'intégration de grandes entreprises comptant plusieurs centaines de collaborateurs posait un problème pratique : envoyer des centaines d'appels SOAP simultanés vers Sabre saturait les quotas de l'API et entraînait des refus en chaîne.

Pour résoudre ce problème, j'ai développé les composants \texttt{DialogUpdateSabre.vue} et \texttt{DialogImportProfil.vue}. Ils découpent l'import en paquets de 50 profils (\textit{chunking}). Si une coupure réseau survient au milieu du traitement, le système ne réexécute que le paquet interrompu au lieu de devoir tout recommencer depuis le début.

\chapter{Tests Automatisés et Résultats en Production}
\label{chap:assurance_qualite}

\section{Suite de Tests Unitaires et d'Intégration}

Pour sécuriser les calculs de prix et les flux d'émission, nous avons mis en place des tests automatisés exécutés sur notre intégration continue (CI/CD) :

La suite \texttt{orderCreation.utils.test.js} (Mocha/Chai) teste l'algorithme d'appairage des passagers (\texttt{matchesPaxRefId}) sur une quarantaine de cas réels : bébés multiples, noms à particules ou suppression des bagages sans voyageur.

La suite \texttt{pricingConsistency.utils.test.js} vérifie l'égalité $\text{Total} = \text{Base} + \sum \text{Taxes}$ sur différentes devises (EUR, USD, TND, JPY) pour valider l'arrondi bancaire. Enfin, \texttt{shopping.utils.test.js} contrôle le bon filtrage des compagnies aériennes et le formatage des fenêtres horaires ISO 8601.

\section{Résultats Mesurés en Production}

Le tableau \ref{tab:comparatif_avant_apres} résume les résultats observés en production avant et après ces développements, d'après nos logs Winston et nos métriques Sentry.

\begin{table}[H]
\centering
\small
\renewcommand{\arraystretch}{1.3}
\arrayrulecolor{CobaltBlue!25}
\begin{tabularx}{\textwidth}{>{\columncolor{CobaltBlue!5}\bfseries}p{3.0cm} p{5.0cm} X}
\hline
\rowcolor{CobaltBlue!12}
\textbf{\color{NavyBlue}Indicateur} & \textbf{\color{NavyBlue}État Initial (Avant Réalisations)} & \textbf{\color{NavyBlue}État Final (Après Déploiement)} \\
\hline
Taux d'Échec Création d'Ordre & 8,4\,\% d'erreurs (rejets par les compagnies pour décalages \texttt{PaxRefID} ou services orphelins). & \textbf{$<$ 0,1\,\%} d'erreurs applicatives (hors indisponibilités compagnies). \\
Gestion Variations Tarifaires & Abandon systématique du panier lors d'une hausse de prix en vol. & \textbf{Flux réactif de reprise (409)} : 92\,\% des paniers réajustés sont finalisés sans ressaisie. \\
Accès Base (Devise POS) & Requêtes SQL bloquantes synchrones à chaque appel de contrôleur. & \textbf{Middleware asynchrone + cache LRU} : suppression du goulot d'étranglement I/O. \\
Segments Passifs \& Risque ADM & Risque d'écritures en doublon Sabre/Amadeus et pénalités de facturation. & \textbf{Découplage strict en base} : zéro litige ADM pour duplication constaté sur 2 ans. \\
Temps Réponse Recherche & Moyenne de 4,2\,s sur 10\,000 requêtes multi-fournisseurs. & \textbf{Réduction à 3,0\,s (-28\,\%)} grâce au filtrage en amont et à l'optimisation des payloads. \\
Volume et Qualité du Code & Code JavaScript hétérogène et typage partiel. & \textbf{+192\,630 lignes nettes} maintenues, 100\,\% typées TypeScript sur le front et testées. \\
\hline
\end{tabularx}
\caption{Synthèse factuelle et chiffrée des impacts d'ingénierie sur la plateforme Metis}
\label{tab:comparatif_avant_apres}
\end{table}
